services:
  - type: web
    name: credit-card-recommender
    env: python
    buildCommand: ""
    startCommand: python app.py
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production

